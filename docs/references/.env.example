# ============================================================================
# SUPABASE CONFIGURATION (Local Development)
# ============================================================================
# After running 'pnpx supabase start', copy the values from the output:
#   - Publishable key → NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
#   - Secret key → SUPABASE_SERVICE_ROLE_KEY & SUPABASE_SERVICE_ROLE_JWT
# For production, get these from: https://app.supabase.com/project/_/settings/api
# ============================================================================

NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_publishable_key_from_supabase_start
S3_ACCESS_KEY=your_s3_access_key_from_supabase_start

# ============================================================================
# INITIAL SETUP CONFIGURATION
# ============================================================================
# Password required to access /setup endpoint for creating the first admin user
# Change this to a secure password before running the application
# ============================================================================

SETUP_PASSWORD=change_this_to_secure_password

# ============================================================================
# DEFAULT ADMIN ACCOUNT
# ============================================================================
# These credentials will be used to create the admin user during initial setup
# You can change these values before running /setup
# ============================================================================

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!
ADMIN_NAME=Administrator

# ============================================================================
# SUPABASE SERVICE ROLE (Server-side only)
# ============================================================================
# Copy 'Secret key' from 'pnpx supabase start' output to both variables below
# These keys bypass Row Level Security - keep them secure and never expose to client
# ============================================================================

SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_from_supabase_start
SUPABASE_SERVICE_ROLE_JWT=your_service_role_jwt_from_supabase_start
